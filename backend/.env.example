NODE_ENV=development
PORT=3001

DB_HOST=localhost
DB_PORT=5432
DB_NAME=pos_kiosko
DB_USER=postgres
DB_PASSWORD=postgres

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Required only when NODE_ENV=production (CORS origin for the frontend)
FRONTEND_URL=https://your-frontend-domain.com

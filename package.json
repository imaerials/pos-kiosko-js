{
  "name": "pos-kiosko",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"API,UI\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:ui": "npm run dev --prefix frontend",
    "seed": "npm run db:seed --prefix backend",
    "seed:users": "npm run db:seed:users --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
